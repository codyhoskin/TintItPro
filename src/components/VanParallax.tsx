import styles from "../styles/VanParallax.module.css";

const VanParallax = () => (
  <section
    className={styles.parallaxSection}
    role="img"
    aria-label="Tint It Pro service van parked outside a modern home"
  />
);

export default VanParallax;
