# Keep the real key in Netlify environment variables or .env.local.
# Enable Places API (New) and restrict the key to that API.
GOOGLE_MAPS_API_KEY=

# Tint It Pro's verified Google Business Profile.
GOOGLE_PLACE_ID=ChIJSeCMo-sKnS4RXSZsamLAv1o

# Optional: paste only the content value from Google Search Console's
# google-site-verification meta tag (not the complete HTML tag).
GOOGLE_SITE_VERIFICATION=
